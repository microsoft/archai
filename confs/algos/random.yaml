# Use darts defaults
__include__: "darts.yaml"

nas:
  search:
    model_desc:
      # Number of edges that each node will take input from
      num_edges_to_sample: 2

  eval:
    model_desc:
      # Number of edges that each node will take input from
      num_edges_to_sample: 2
