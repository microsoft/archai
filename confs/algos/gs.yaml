__include__: "darts.yaml" # just use darts defaults

nas:

  eval:
    loader:
      train_batch: 64

  search:
    gs:
      num_sample: 4
    loader:
      val_ratio: 0.0 # don't need val during search
    trainer:
      epochs: 50
    model_desc:
      max_final_edges: 4
