dataset:
  # Folder where directory for each dataset exist,
  # empty string means chose default based on OS which is typically ~/dataroot
  dataroot: "$default_dataroot"

dataset_eval:
  # Folder where directory for each dataset exist,
  # empty string means chose default based on OS which is typically ~/dataroot
  dataroot: "$default_dataroot"
